import React, { Component } from "react"

//component for the game title.

class GameTitle extends Component {
  render() {
    return (
      <div>
        <h1 id="gameTitle">match.Line</h1>
      </div>
    )
  }
}

export default GameTitle
